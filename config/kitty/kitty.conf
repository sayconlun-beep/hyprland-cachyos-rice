# kitty. Colours are colors.conf, generated from the wallpaper by matugen;
# rice-theme-reload sends SIGUSR1, which makes running kittys re-read this.
background_opacity          0.6
cursor_trail                1
window_padding_width        25

include colors.conf
