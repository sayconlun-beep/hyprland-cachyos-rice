// A glass card for the dashboard, matching the control centre's tiles.
import QtQuick

Rectangle {
    radius: 22
    color: Theme.alpha(Theme.surface_container_high, 0.85)
    border.width: 1
    border.color: Theme.alpha(Theme.outline_variant, 0.45)
}
