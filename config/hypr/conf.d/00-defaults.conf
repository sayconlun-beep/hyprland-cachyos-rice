# Drop-in overrides, sourced at the very end of hyprland.conf.
#
#   10-monitors.conf   written by rice-monitor-ctl.py  (rice-settings displays)
#   20-look.conf       written by rice-look-ctl.py     (rice-settings look)
#
# This file exists so the `conf.d/*.conf` glob always matches something on a
# fresh checkout. It is otherwise intentionally empty.
